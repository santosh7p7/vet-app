<!DOCTYPE html>
<html>
<head>
  <title>Humans Best Friends Veterinary Clinic</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">  
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application", "canvasjs.min"%>
  <%= yield :js_code %>
  <%= csrf_meta_tags %>   
  <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
  <!-- [if lt IE 9]  
         <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif] -->
</head>


<body  id='bodytop' class="Body">
<div id="main" role="main">
  <div class="container-fluid">
    <div class="content">
     <% if flash and flash.keys.size > 0 %>
      <div class="row-fluid">
         <div class="span8">
           <% flash.each do |key, value| %>
             <div class='alert alert-<%= key == :notice ? "success" : "error"  %>'><%= value %></div>
           <% end %>
         </div>
      </div>
      <% end %>
      <div class="row-fluid">
         <div class="span12"><%= yield %></div>
      </div>
    </div>
  </div>
</div>
</body>
</html>
